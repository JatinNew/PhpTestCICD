pipeline {
    agent any

    stages {
        stage('Deploy PHP application') {
            steps {
                sh 'scp -r ${WORKSPACE}/* root@https://www.qa.ps.isvplugins.com:index.php
            }
        }
    }
}
